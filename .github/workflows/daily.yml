name: daily-arxiv-digest

on:
  schedule:
    # 周一到周五 08:00 America/Los_Angeles。cron 用 UTC；15:00 UTC = 08:00 PDT(夏令时)。
    # 1-5 = Mon–Fri。冬令时(PST, UTC-8)时改成 "0 16 * * 1-5"。
    - cron: "0 15 * * 1-5"
  workflow_dispatch: {}   # 手动触发

permissions:
  contents: write          # 把 feedback.jsonl commit 回来
  issues: write            # 建 digest issue + 评论、读评论 reaction 当反馈

jobs:
  digest:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - name: Ingest 👍/👎 (issue reactions) + rank + email + post issue
        env:
          LLM_PROVIDER: ${{ secrets.LLM_PROVIDER }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_MODEL: claude-opus-4-8
          SEED_AUTHOR: ${{ secrets.SEED_AUTHOR }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}   # 自动注入，权限见上面 permissions
          # GITHUB_REPOSITORY 由 Actions 运行环境自动提供（owner/repo）
          SMTP_SENDER_EMAIL: ${{ secrets.SMTP_SENDER_EMAIL }}
          SMTP_APP_PASSWORD: ${{ secrets.SMTP_APP_PASSWORD }}
          DIGEST_RECIPIENT: ${{ secrets.DIGEST_RECIPIENT }}
        run: |
          python arxiv_daily.py --cats cs.CL,cs.LG,cs.AI --pool 250 --top 7
      - name: Persist feedback state
        run: |
          git config user.name "JamesSand"
          git config user.email "zhizhousha@together.ai"
          touch feedback.jsonl
          git add feedback.jsonl
          git diff --cached --quiet || (git commit -m "update feedback state [skip ci]" && git push)
